<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BodyReportMobile.Presenter.Pages" 
             x:Class="BodyReportMobile.Presenter.Pages.RegisterAccountPage"
             Title="{Binding TitleLabel}">
  <ContentPage.Content>
  </ContentPage.Content>
  
  <Grid>
    <AbsoluteLayout>
      <ActivityIndicator AbsoluteLayout.LayoutFlags="PositionProportional"
                         AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1" IsRunning="{Binding ActionIsInProgress}" IsVisible="{Binding ActionIsInProgess}" Color="Red" />
    </AbsoluteLayout>

    <StackLayout Orientation="Vertical" Margin="20">
      <Entry Placeholder="{Binding UserNameLabel}" Text="{Binding UserName}"/>
      <Entry Placeholder="{Binding EmailLabel}" Text="{Binding Email}"/>
      <Entry Placeholder="{Binding PasswordLabel}" Text="{Binding Password}" IsPassword="True"/>
      <Entry Placeholder="{Binding ConfirmPasswordLabel}" Text="{Binding ConfirmPassword}" IsPassword="True"/>

      <Button Text="{Binding RegisterLabel}" Command="{Binding RegisterAccountCommand}" HorizontalOptions="FillAndExpand"/>
    </StackLayout>
  </Grid>
</local:BaseContentPage>